# /etc/ipsec.secrets - strongSwan IPsec secrets file

<%= node[:strongswan][:ipsec][:remote][:id] %> : PSK "<%= node[:strongswan][:ipsec][:psk] %>"

# This is setup for either of us to test. Normally would only have one user listed unless the machine had access from multiple accounts; will ponder that last
# <xauth_username> : XAUTH "<xauth_password>"
