# /etc/ipsec.secrets - strongSwan IPsec secrets file

: PSK "<%= node[:strongswan][:server][:tunable][:ipsec][:psk] %>"
# This is setup for either of us to test. Normally would only have one user listed unless the machine had access from multiple accounts; will ponder that last bit and come up with a solution.
schade : XAUTH "<%= node[:strongswan][:server][:tunable][:ipsec][:psk] %>"
temujin9 : XAUTH "<%= node[:strongswan][:server][:tunable][:ipsec][:psk] %>"
# <xauth_username> : XAUTH "<xauth_password>"
