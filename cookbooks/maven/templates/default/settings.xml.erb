<settings>
  <servers>

    <!--

	 The correct way to do this may be to finish the planned
	 maven_repo provider (see github.com/opscode-cookbooks). That
	 will allow a user to add an arbitrary repository to this
	 file.

    -->

    <server>
      <id>infochimps.repo</id>
      <username><%= @aws_access_key %></username>
      <passphrase><%= @aws_secret_access_key %></passphrase>
    </server>
  </servers>
</settings>
