# THIS FILE HAS BEEN GENERATED BY CHEF
# ANY MANUAL MODIFICATIONS WILL BE OVERWRITTEN
# /etc/mongodb/mongodb.conf

# Where to store the data.

# Note: if you run mongodb as a non-root user (recommended) you may
# need to create and set permissions for this directory manually,
# e.g., if the parent directory isn't mutable by the mongodb user.

dbpath = <%= @node[:mongodb][:data_dir] %>
logpath = <%= @log_path %>
pidfilepath = <%= @node[:mongodb][:pid_dir] %>/mongodb.pid
port = <%= @node[:mongodb][:server][:mongod_port] %>
fork = true

<% if @node[:mongodb][:server][:bind_ip] %>
bind_ip = <%= @node[:mongodb][:server][:bind_ip] %>
<% end %> 
