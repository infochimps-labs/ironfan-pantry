#!/bin/bash

exec 2>&1

cd <%= node[:package_server][:gem][:home_dir] %>
exec chpst -u <%= node[:package_server][:user] %> \
  unicorn -c <%= node[:package_server][:gem][:conf_dir] %>/unicorn.rb -p <%= node[:package_server][:gem][:port] %>
