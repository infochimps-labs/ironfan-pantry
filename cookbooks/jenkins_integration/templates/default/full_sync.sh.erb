#!/usr/bin/env bash
#
# Synchronize local code to the Chef server.
#
source knife_shared.inc

cat <<EOF > config/Berksfile.conf.rb
PANTRY_BRANCH='testing'
ENTERPRISE_BRANCH='testing'
EOF

# Would do rake full_sync, but that also syncs all clusters (redundant & slow)
rake roles
rake berkshelf_install
