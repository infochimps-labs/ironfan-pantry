#!/usr/bin/env bash
#
# Launch a testing instance instance
#
source `dirname $0`/shared.inc
export FACET=<%= node[:jenkins_integration][:ironfan_ci][:target] %>
pushd <%= node[:jenkins_integration][:ironfan_ci][:test_homebase] %>
source $ROOT_DIR/launch.inc
popd