#!/usr/bin/env bash
#
# Synchronize code that has changed to the Chef server.
#
source `dirname $0`/knife_shared.inc

cat <<EOF > config/Berksfile.conf.rb
PANTRY_BRANCH='testing'
ENTERPRISE_BRANCH='testing'
EOF

rake roles
rake berkshelf
echo "FIXME: Need to upload *just* the changed cookbooks"
