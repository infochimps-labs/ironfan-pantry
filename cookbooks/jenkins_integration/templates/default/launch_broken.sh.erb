#!/usr/bin/env bash
#
# Launch a known-broken instance
#
source `dirname $0`/shared.inc

switch_to $TEST_PATH

$ROOT_DIR/launch.sh $BROKEN

if [ $? == 2 ]; then
  echo "Failed as expected"
  exit 0
else
  echo "Unexpected failure type!"
  exit 1
fi

