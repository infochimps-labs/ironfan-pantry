#!/usr/bin/env bash
#
# Install all dependencies via bundler.
#
source `dirname $0`/shared.inc

if [ ! -f Gemfile.lock ]; then
  echo "Installing bundle:"
  time bundle install --path vendor --no-color
else
  echo "Updating bundle:"
  time bundle update --no-color
fi