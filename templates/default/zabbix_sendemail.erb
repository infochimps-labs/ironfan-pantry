#!/bin/bash
# Generated by Chef

SMTP_FROM=<%= node.zabbix.smtp.from %>
SMTP_SERVER=<%= node.zabbix.smtp.server %>:<%= node.zabbix.smtp.port %>
SMTP_USERNAME=<%= node.zabbix.smtp.username %>
SMTP_PASSWORD=<%= node.zabbix.smtp.password %>

# echo From: $SMTP_FROM
# echo To: $1
# echo Subject: $2
# echo Auth: $SMTP_USERNAME identified by $SMTP_PASSWORD at $SMTP_SERVER
# echo $3

/usr/bin/sendEmail -f "$SMTP_FROM" -t "$1" -u "$2" -s "$SMTP_SERVER" -xu "$SMTP_USERNAME" -xp "$SMTP_PASSWORD" -o "tls=yes" -m "$3"
